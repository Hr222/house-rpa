# 贝壳(ke.com)询价 MVP 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 Playwright 实现贝壳深圳站的询价 MVP——常驻浏览器、人工登录后保持会话，接收后端请求 `{小区名, 面积}`，完成「搜小区→筛面积→抓报价→进详情页抓小区均价与成交记录→算最终单价」，返回结构化结果。

**Architecture:** 单浏览器实例常驻的服务（FastAPI）。启动时打开贝壳，人工登录后服务进入就绪态，等待 HTTP 请求。每个请求复用同一登录会话执行采集流程，结果按需求 §3.6 算法计算后返回。采集流程采用「新标签页停留 60s 模拟真人浏览」策略规避风控。算法与解析逻辑与平台 adapter 解耦——纯函数可单测，平台 adapter 仅负责"拿原始数据"。

**Tech Stack:** Python 3.14、Playwright 1.61（已装）、FastAPI（HTTP 服务）、Pydantic（数据模型）、pytest（测试，纯算法部分）

---

## 实测依据（写代码时对照）

### 反爬实测结论
| 页面 | 直连/翻页 | 结果 |
|---|---|---|
| 首页 `/` | — | ✅ 放行 |
| 二手房搜索 `/ershoufang/a3c{id}/` | 第1页 | ✅ 放行（带登录态） |
| 搜索翻页（pg2 起） | — | ❌ 验证码 |
| 小区详情 `/xiaoqu/{id}/` | 常驻会话 | ✅ 放行（被反复折腾的会话除外） |
| 成交记录 `/chengjiao/c{id}/` | — | ❌ 验证码（但详情页内直接显示成交，不需访问此 URL） |

→ 结论：**走常驻会话 + 详情页内成交记录**，绕开成交记录独立页的风控。

### DOM 结构（已实测，来自 ke_rendered.html）
```
搜索结果页 ul.sellListContent 下：
  <li class="clear">                       ← 真实房源（抓）
  <li class="list_goodhouse_daoliu ...">   ← 广告（pass）
  「猜你喜欢」在 sellListContent 之外        ← 到此结束

真实房源字段：
  单价: div.unitPrice span         → "84,547元/平"
  总价: div.totalPrice span        → "720"（万）
  信息: div.houseInfo              → "中楼层 (共44层) | 2015年 | 3室2厅 | 85.16平米 | 西南"
  小区详情链接: a.agentCardResblockLink → href="https://sz.ke.com/xiaoqu/{id}/"

详情页：
  小区均价: 在详情页内，包含「参考均价」「元/㎡」文本（class 含 xiaoquPrice/junjia，按实际微调）
  成交记录: 在详情页内直接显示，每条含成交价/单价/面积/日期（class/结构按实际微调）
```

### 算法（需求 §3.6，原样落地）
```
有 P_deal：
  diff = |P_quote - P_deal| / P_deal
  diff ≤ 10% → min(P_quote, P_deal)    # 取低
  diff > 10% → P_deal                    # 只取成交价
无 P_deal：
  P_quote × 0.8                          # 报价 8 折
```
- 报价/成交面积筛选：`[A×0.8, A×1.2]`（A=后端传入基准面积），代码内过滤
- P_quote：MVP 用详情页「小区均价」（你已拍板）
- P_deal：详情页成交记录，按面积±20%筛选后求算术平均
- 平台容错：MVP 单平台，无跨平台汇总；后续平台接进来往同一池子加

---

## 文件结构

```
jeethink-rpa/
├── app/
│   ├── __init__.py
│   ├── models.py          # 数据模型（Pydantic）：请求/响应/房源/成交记录
│   ├── algorithm.py       # 纯算法：面积筛选、均值计算、§3.6 取舍（无 IO，可单测）
│   ├── parsers.py         # HTML 解析：从页面元素抽房源/成交记录/小区均价（纯函数）
│   ├── ke_adapter.py      # 贝壳 adapter：用 Playwright 执行"搜→抓"流程，调用 parsers
│   ├── browser.py         # 浏览器生命周期管理：启动/登录等待/常驻/保活/健康检查
│   └── server.py          # FastAPI 服务：/inquiry 接口，编排 adapter + algorithm
├── tests/
│   ├── __init__.py
│   ├── test_algorithm.py  # 算法单测（核心，TDD 先行）
│   ├── test_parsers.py    # 解析器单测（用 ke_rendered.html 固定样本）
│   └── fixtures/
│       └── ke_listing_sample.html   # 从 ke_rendered.html 截取的房源片段（测试样本）
├── config.py              # 配置：URL/超时/保活间隔/停留时长
├── main.py                # 入口：启动浏览器 + FastAPI
├── requirements.txt       # 依赖清单
└── docs/
    └── 2026-07-09-ke-mvp.md  # 本计划
```

**设计原则**：`algorithm.py` 和 `parsers.py` 是纯函数，不碰浏览器、不碰网络，**可单测**。`ke_adapter.py` 负责驱动浏览器拿原始数据交给它们。这样核心逻辑（算错就出大事的部分）有测试保护，浏览器交互（易变、难测）保持薄。

---

## Task 1: 项目骨架与依赖

**Files:**
- Create: `jeethink-rpa/requirements.txt`
- Create: `jeethink-rpa/config.py`
- Create: `jeethink-rpa/app/__init__.py` (空)
- Create: `jeethink-rpa/tests/__init__.py` (空)

- [ ] **Step 1: 创建依赖清单**

`requirements.txt`：
```
playwright==1.61.0
fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.8.0
pytest==8.3.0
httpx==0.27.0
```

- [ ] **Step 2: 安装依赖**

Run: `cd jeethink-rpa && pip install -r requirements.txt`
Expected: 全部安装成功

- [ ] **Step 3: 创建配置文件**

`config.py`：
```python
# -*- coding: utf-8 -*-
"""MVP 配置。敏感项(账号)不在此文件，登录走人工。"""

# 贝壳深圳站
KE_HOME = "https://sz.ke.com/"
KE_CITY = "sz"

# 风控规避参数
DETAIL_TAB_LINGER_SECONDS = 60     # 详情页标签停留时长（模拟真人浏览）
KEEPALIVE_INTERVAL_SECONDS = 600   # 保活间隔（10 分钟）
REQUEST_TIMEOUT_SECONDS = 60       # 单次询价超时

# 算法参数（需求 §3.6）
AREA_TOLERANCE = 0.20              # 面积 ±20%
DISCOUNT_WHEN_NO_DEAL = 0.8        # 无成交时报价 8 折
DEAL_DIFF_THRESHOLD = 0.10         # 报价/成交差异 10% 阈值
```

- [ ] **Step 4: 创建空 __init__.py**

`app/__init__.py` 和 `tests/__init__.py` 均为空文件。

- [ ] **Step 5: Commit**

```bash
cd jeethink-rpa
git add requirements.txt config.py app/__init__.py tests/__init__.py
git commit -m "feat(rpa): 项目骨架与依赖"
```

---

## Task 2: 数据模型

**Files:**
- Create: `jeethink-rpa/app/models.py`

- [ ] **Step 1: 创建数据模型**

`app/models.py`：
```python
# -*- coding: utf-8 -*-
"""数据模型。与平台无关，所有 adapter 共用。"""
from typing import List, Optional, Literal
from pydantic import BaseModel, Field


class InquiryRequest(BaseModel):
    """后端 → RPA 的请求。"""
    community_name: str = Field(..., description="小区名称")
    area: float = Field(..., gt=0, description="基准面积(㎡)")
    city: str = Field("深圳", description="城市，MVP 固定深圳")


class Listing(BaseModel):
    """单条在售房源（报价数据）。"""
    unit_price: Optional[float] = Field(None, description="报价单价(元/㎡)")
    total_price: Optional[float] = Field(None, description="总价(万)")
    area: Optional[float] = Field(None, description="面积(㎡)")


class DealRecord(BaseModel):
    """单条成交记录。"""
    unit_price: Optional[float] = Field(None, description="成交单价(元/㎡)")
    area: Optional[float] = Field(None, description="面积(㎡)")


class PlatformResult(BaseModel):
    """单平台采集结果。"""
    name: str
    status: Literal["SUCCESS", "NO_DATA", "BLOCKED", "TIMEOUT", "ERROR", "LOGIN_EXPIRED"]
    community_avg_price: Optional[float] = Field(None, description="详情页小区均价(元/㎡)")
    listings: List[Listing] = Field(default_factory=list, description="在售房源")
    deals: List[DealRecord] = Field(default_factory=list, description="成交记录")
    reason: Optional[str] = None


AlgorithmBranch = Literal["TAKE_LOWER", "DEAL_ONLY", "QUOTE_DISCOUNT", "FAILED"]


class InquiryResponse(BaseModel):
    """RPA → 后端的响应。"""
    success: bool
    final_price: Optional[float] = Field(None, description="最终建议单价(元/㎡)")
    algorithm_branch: AlgorithmBranch
    quote_avg: Optional[float] = None
    deal_avg: Optional[float] = None
    platforms: List[PlatformResult] = Field(default_factory=list)
    elapsed_ms: int = 0
```

- [ ] **Step 2: 验证模型可导入**

Run: `cd jeethink-rpa && python -c "from app.models import InquiryRequest, InquiryResponse; print('OK')"`
Expected: 输出 `OK`

- [ ] **Step 3: Commit**

```bash
git add app/models.py
git commit -m "feat(rpa): 数据模型(请求/响应/房源/成交)"
```

---

## Task 3: 核心算法（TDD 先行）

这是算错就出大事的部分，用 TDD 保护。纯函数，无 IO。

**Files:**
- Create: `jeethink-rpa/tests/test_algorithm.py`
- Create: `jeethink-rpa/app/algorithm.py`

- [ ] **Step 1: 写失败测试**

`tests/test_algorithm.py`：
```python
# -*- coding: utf-8 -*-
import pytest
from app.algorithm import filter_by_area, mean_price, decide_final_price


class TestFilterByArea:
    def test_keeps_within_tolerance(self):
        listings = [{"area": 100}, {"area": 110}, {"area": 90}]
        result = filter_by_area(listings, base_area=100, tolerance=0.20)
        assert len(result) == 3

    def test_filters_outside_tolerance(self):
        listings = [{"area": 70}, {"area": 100}, {"area": 130}]
        result = filter_by_area(listings, base_area=100, tolerance=0.20)
        assert len(result) == 1
        assert result[0]["area"] == 100

    def test_boundary_inclusive(self):
        # 80 和 120 是边界(100*0.8, 100*1.2)，应保留
        listings = [{"area": 80}, {"area": 120}]
        result = filter_by_area(listings, base_area=100, tolerance=0.20)
        assert len(result) == 2

    def test_none_area_excluded(self):
        listings = [{"area": None}, {"area": 100}]
        result = filter_by_area(listings, base_area=100, tolerance=0.20)
        assert len(result) == 1


class TestMeanPrice:
    def test_simple_mean(self):
        assert mean_price([{"unit_price": 100}, {"unit_price": 200}]) == 150.0

    def test_skips_none(self):
        assert mean_price([{"unit_price": None}, {"unit_price": 300}]) == 300.0

    def test_empty_returns_none(self):
        assert mean_price([]) is None


class TestDecideFinalPrice:
    def test_diff_within_10pct_take_lower(self):
        # quote=100, deal=105, diff=4.76% <=10% → min=100
        result = decide_final_price(quote_avg=100, deal_avg=105)
        assert result.final_price == 100
        assert result.branch == "TAKE_LOWER"

    def test_diff_over_10pct_take_deal_only(self):
        # quote=100, deal=130, diff=23% >10% → deal=130
        result = decide_final_price(quote_avg=100, deal_avg=130)
        assert result.final_price == 130
        assert result.branch == "DEAL_ONLY"

    def test_no_deal_discount_quote(self):
        # 无成交 → 100 * 0.8 = 80
        result = decide_final_price(quote_avg=100, deal_avg=None)
        assert result.final_price == 80.0
        assert result.branch == "QUOTE_DISCOUNT"

    def test_no_quote_no_deal_failed(self):
        result = decide_final_price(quote_avg=None, deal_avg=None)
        assert result.final_price is None
        assert result.branch == "FAILED"

    def test_no_quote_but_has_deal(self):
        # 无报价但有成交 → 取成交价
        result = decide_final_price(quote_avg=None, deal_avg=100)
        assert result.final_price == 100
        assert result.branch == "DEAL_ONLY"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd jeethink-rpa && python -m pytest tests/test_algorithm.py -v`
Expected: FAIL（ModuleNotFoundError: app.algorithm）

- [ ] **Step 3: 实现算法**

`app/algorithm.py`：
```python
# -*- coding: utf-8 -*-
"""询价算法（纯函数，无 IO）。需求 §3.6。"""
from typing import List, Optional, Dict, Any
from dataclasses import dataclass


@dataclass
class Decision:
    """取舍决策结果。"""
    final_price: Optional[float]
    branch: str  # TAKE_LOWER / DEAL_ONLY / QUOTE_DISCOUNT / FAILED


def filter_by_area(items: List[Dict[str, Any]], base_area: float,
                   tolerance: float = 0.20) -> List[Dict[str, Any]]:
    """按基准面积 ±tolerance 过滤。面积缺失(None)的剔除。闭区间。"""
    low = base_area * (1 - tolerance)
    high = base_area * (1 + tolerance)
    return [it for it in items
            if it.get("area") is not None and low <= it["area"] <= high]


def mean_price(items: List[Dict[str, Any]]) -> Optional[float]:
    """对 unit_price 字段求算术平均。无有效值返回 None。"""
    prices = [it["unit_price"] for it in items
              if it.get("unit_price") is not None]
    if not prices:
        return None
    return sum(prices) / len(prices)


def decide_final_price(quote_avg: Optional[float],
                       deal_avg: Optional[float],
                       diff_threshold: float = 0.10,
                       no_deal_discount: float = 0.8) -> Decision:
    """§3.6 取舍算法。

    - 无报价无成交 → FAILED
    - 无成交 → 报价 × 折扣 (QUOTE_DISCOUNT)
    - 无报价有成交 → 成交价 (DEAL_ONLY)
    - 差异 ≤ 阈值 → 取低 (TAKE_LOWER)
    - 差异 > 阈值 → 只取成交 (DEAL_ONLY)
    """
    if quote_avg is None and deal_avg is None:
        return Decision(final_price=None, branch="FAILED")

    if deal_avg is None:
        return Decision(final_price=quote_avg * no_deal_discount,
                        branch="QUOTE_DISCOUNT")

    if quote_avg is None:
        return Decision(final_price=deal_avg, branch="DEAL_ONLY")

    diff = abs(quote_avg - deal_avg) / deal_avg
    if diff <= diff_threshold:
        return Decision(final_price=min(quote_avg, deal_avg),
                        branch="TAKE_LOWER")
    return Decision(final_price=deal_avg, branch="DEAL_ONLY")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd jeethink-rpa && python -m pytest tests/test_algorithm.py -v`
Expected: 11 passed

- [ ] **Step 5: Commit**

```bash
git add app/algorithm.py tests/test_algorithm.py
git commit -m "feat(rpa): 核心算法-面积筛选/均值/§3.6取舍(TDD)"
```

---

## Task 4: HTML 解析器（用实测样本 TDD）

**Files:**
- Create: `jeethink-rpa/tests/fixtures/ke_listing_sample.html`（从 ke_rendered.html 截取 2-3 个 li）
- Create: `jeethink-rpa/tests/test_parsers.py`
- Create: `jeethink-rpa/app/parsers.py`

- [ ] **Step 1: 准备测试样本**

从已存的 `ke_rendered.html` 截取 `ul.sellListContent` 片段作为测试 fixture：
```bash
cd jeethink-rpa
python -c "
import re
from pathlib import Path
html = Path('ke_rendered.html').read_text(encoding='utf-8')
m = re.search(r'<ul[^>]*sellListContent[^>]*>.*?</ul>', html, re.S)
Path('tests/fixtures/ke_listing_sample.html').write_text(
    '<html><body>' + (m.group(0) if m else '') + '</body></html>', encoding='utf-8')
print('样本已生成')
"
```

- [ ] **Step 2: 写失败测试**

`tests/test_parsers.py`：
```python
# -*- coding: utf-8 -*-
from pathlib import Path
from app.parsers import parse_listings, is_ad, is_guess_you_like_boundary

FIXTURE = Path(__file__).parent / "fixtures" / "ke_listing_sample.html"


def load_fixture():
    return FIXTURE.read_text(encoding="utf-8")


class TestParseListings:
    def test_extracts_unit_price(self):
        listings = parse_listings(load_fixture())
        assert len(listings) > 0
        # 实测样本第一条单价 84547
        assert listings[0]["unit_price"] == 84547.0

    def test_extracts_area(self):
        listings = parse_listings(load_fixture())
        assert listings[0]["area"] == 85.16

    def test_excludes_ads(self):
        """广告 li(list_goodhouse_daoliu) 不应出现。"""
        listings = parse_listings(load_fixture())
        # 样本里有 26 个真实房源 + 1 个广告，应得 26
        assert len(listings) == 26


class TestIsAd:
    def test_daoliu_is_ad(self):
        assert is_ad('<li class="list_goodhouse_daoliu VIEWDATA">') is True

    def test_clear_is_not_ad(self):
        assert is_ad('<li class="clear">') is False
```

- [ ] **Step 3: 运行确认失败**

Run: `cd jeethink-rpa && python -m pytest tests/test_parsers.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 4: 实现解析器**

`app/parsers.py`：
```python
# -*- coding: utf-8 -*-
"""贝壳 HTML 解析器（纯函数）。基于实测 DOM 结构。

DOM 结构(实测)：
  真实房源: <li class="clear">
  广告:     <li class="list_goodhouse_daoliu ...">
  单价:     div.unitPrice span       → "84,547元/平"
  总价:     div.totalPrice span      → "720"
  信息:     div.houseInfo            → "... | 85.16平米 | ..."
"""
import re
from typing import List, Dict, Any, Optional

try:
    from bs4 import BeautifulSoup
    _HAS_BS4 = True
except ImportError:
    _HAS_BS4 = False


def _to_float(text: str) -> Optional[float]:
    """'84,547元/平' → 84547.0；提取失败返回 None。"""
    if not text:
        return None
    m = re.search(r"[\d,]+\.?\d*", text)
    if not m:
        return None
    return float(m.group(0).replace(",", ""))


def _extract_area(house_info_text: str) -> Optional[float]:
    """'... | 85.16平米 | ...' → 85.16。"""
    if not house_info_text:
        return None
    m = re.search(r"([\d.]+)\s*平米", house_info_text)
    return float(m.group(1)) if m else None


def is_ad(li_html: str) -> bool:
    """判断是否广告/导流位。"""
    return "list_goodhouse_daoliu" in li_html or "goodhouse" in li_html.lower()


def is_guess_you_like_boundary(text: str) -> bool:
    """判断是否「猜你喜欢」分界（sellListContent 之外，到此结束）。"""
    return "猜你喜欢" in text or "猜您喜欢" in text


def parse_listings(html: str) -> List[Dict[str, Any]]:
    """从搜索结果页 HTML 解析在售房源。自动过滤广告。

    Returns: [{"unit_price", "total_price", "area"}, ...]
    """
    if _HAS_BS4:
        return _parse_with_bs4(html)
    return _parse_with_regex(html)


def _parse_with_bs4(html: str) -> List[Dict[str, Any]]:
    soup = BeautifulSoup(html, "html.parser")
    results = []
    for li in soup.select("ul.sellListContent > li"):
        cls = li.get("class", [])
        if is_ad(" ".join(cls)):
            continue
        unit_el = li.select_one("div.unitPrice span")
        total_el = li.select_one("div.totalPrice span")
        info_el = li.select_one("div.houseInfo")
        results.append({
            "unit_price": _to_float(unit_el.get_text() if unit_el else None),
            "total_price": _to_float(total_el.get_text() if total_el else None),
            "area": _extract_area(info_el.get_text() if info_el else None),
        })
    return results


def _parse_with_regex(html: str) -> List[Dict[str, Any]]:
    """无 bs4 时的正则兜底解析。"""
    results = []
    for m in re.finditer(r'<li[^>]*class="clear"[^>]*>(.*?)</li>', html, re.S):
        block = m.group(0)
        unit = re.search(r'<div class="unitPrice"[^>]*>\s*<span>([^<]*)</span>', block)
        total = re.search(r'<div class="totalPrice[^"]*"[^>]*>\s*<span[^>]*>([^<]*)</span>', block)
        info = re.search(r'<div class="houseInfo"[^>]*>(.*?)</div>', block, re.S)
        results.append({
            "unit_price": _to_float(unit.group(1) if unit else None),
            "total_price": _to_float(total.group(1) if total else None),
            "area": _extract_area(re.sub(r"<[^>]+>", " ", info.group(1)) if info else None),
        })
    return results


def find_detail_link(html: str) -> Optional[str]:
    """从搜索结果页提取「查看小区详情」链接。"""
    m = re.search(r'<a[^>]*class="[^"]*agentCardResblockLink[^"]*"[^>]*href="([^"]+)"', html)
    if m:
        return m.group(1)
    m = re.search(r'<a[^>]*href="(https?://sz\.ke\.com/xiaoqu/\d+/)"[^>]*>查看小区详情', html)
    return m.group(1) if m else None
```

- [ ] **Step 5: 安装 bs4 并运行测试**

Run:
```bash
cd jeethink-rpa
pip install beautifulsoup4 lxml
python -m pytest tests/test_parsers.py -v
```
Expected: 5 passed

- [ ] **Step 6: 把 bs4 加入依赖**

在 `requirements.txt` 追加：
```
beautifulsoup4==4.12.0
lxml==5.2.0
```

- [ ] **Step 7: Commit**

```bash
git add app/parsers.py tests/test_parsers.py tests/fixtures/ requirements.txt
git commit -m "feat(rpa): HTML解析器-房源/广告/详情链接(bs4+正则兜底)"
```

---

## Task 5: 成交记录解析（详情页）

详情页的成交记录结构因我的测试会话被风控无法实测，解析器写稳健的文本兜底，实际 class 在常驻会话登录后微调。

**Files:**
- Modify: `jeethink-rpa/app/parsers.py`（追加函数）
- Create: `jeethink-rpa/tests/test_parsers_deals.py`

- [ ] **Step 1: 写测试（用模拟样本）**

`tests/test_parsers_deals.py`：
```python
# -*- coding: utf-8 -*-
from app.parsers import parse_deals, parse_community_avg_price

# 模拟详情页成交记录片段（实际 class 在常驻会话微调，解析用稳健文本匹配）
DEAL_SAMPLE = """
<ul class="list-content">
  <li>
    <div class="info">
      <div class="title">3室2厅 | 85.16平米 | 南北 | 中楼层</div>
      <div class="dealDate">2026-06</div>
    </div>
    <div class="flood">
      <div class="totalPrice"><span>720</span>万</div>
      <div class="unitPrice"><span>84,547元/平</span></div>
    </div>
  </li>
  <li>
    <div class="info">
      <div class="title">2室1厅 | 75.5平米 | 南 | 高楼层</div>
    </div>
    <div class="unitPrice"><span>90,000元/平</span></div>
  </li>
</ul>
<div class="xiaoquPrice"><span class="junjia">85,635</span>元/㎡</div>
"""

class TestParseDeals:
    def test_extracts_unit_prices(self):
        deals = parse_deals(DEAL_SAMPLE)
        assert len(deals) >= 1
        prices = [d["unit_price"] for d in deals if d["unit_price"]]
        assert 84547.0 in prices

    def test_extracts_area(self):
        deals = parse_deals(DEAL_SAMPLE)
        areas = [d["area"] for d in deals if d["area"]]
        assert 85.16 in areas

    def test_empty_html(self):
        assert parse_deals("<html></html>") == []


class TestParseCommunityAvgPrice:
    def test_extracts_avg(self):
        price = parse_community_avg_price(DEAL_SAMPLE)
        assert price == 85635.0

    def test_none_when_missing(self):
        assert parse_community_avg_price("<html></html>") is None
```

- [ ] **Step 2: 运行确认失败**

Run: `cd jeethink-rpa && python -m pytest tests/test_parsers_deals.py -v`
Expected: FAIL（函数不存在）

- [ ] **Step 3: 在 parsers.py 追加成交解析函数**

在 `app/parsers.py` 末尾追加：
```python
def parse_deals(html: str) -> List[Dict[str, Any]]:
    """从详情页解析成交记录。用稳健文本匹配，不依赖固定 class。

    策略：找所有「XX,XXX元/平」附近的面积。
    """
    results = []
    if _HAS_BS4:
        soup = BeautifulSoup(html, "html.parser")
        # 详情页成交记录通常在含「成交」的区域，每条有单价+面积
        for li in soup.select("li"):
            text = li.get_text(separator=" ", strip=True)
            if "元/平" not in text and "元/㎡" not in text:
                continue
            unit = _to_float(_find_price_text(text))
            area = _extract_area(text)
            if unit or area:
                results.append({"unit_price": unit, "area": area})
    else:
        for m in re.finditer(r"<li[^>]*>(.*?)</li>", html, re.S):
            text = re.sub(r"<[^>]+>", " ", m.group(1))
            if "元/平" not in text and "元/㎡" not in text:
                continue
            results.append({"unit_price": _to_float(_find_price_text(text)),
                            "area": _extract_area(text)})
    return results


def _find_price_text(text: str) -> Optional[str]:
    """从文本中找 'XX,XXX元/平' 格式的单价串。"""
    m = re.search(r"[\d,]+\.?\d*\s*元/[平㎡]", text)
    return m.group(0) if m else None


def parse_community_avg_price(html: str) -> Optional[float]:
    """从详情页解析小区均价（'XX,XXX元/㎡' 或 参考均价）。"""
    if _HAS_BS4:
        soup = BeautifulSoup(html, "html.parser")
        for el in soup.select(".xiaoquPrice, .junjia, [class*='unitPrice']"):
            t = _find_price_text(el.get_text() or "")
            if t:
                return _to_float(t)
    m = re.search(r"([\d,]+\.?\d*)\s*元/[平㎡]", html)
    return _to_float(m.group(0)) if m else None
```

- [ ] **Step 4: 运行测试**

Run: `cd jeethink-rpa && python -m pytest tests/test_parsers_deals.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add app/parsers.py tests/test_parsers_deals.py
git commit -m "feat(rpa): 成交记录与小区均价解析(文本兜底)"
```

---

## Task 6: 贝壳 Adapter（浏览器驱动）

把 Playwright 操作串起来。这是唯一碰浏览器的业务文件。

**Files:**
- Create: `jeethink-rpa/app/ke_adapter.py`

- [ ] **Step 1: 实现 adapter**

`app/ke_adapter.py`：
```python
# -*- coding: utf-8 -*-
"""贝壳 adapter：用 Playwright 执行采集流程。

流程：
  1. 搜索页：小区名+面积筛选 → 抓在售房源 + 找详情链接
  2. 新标签页打开详情页 → 抓小区均价 + 成交记录
  3. 标签页停留 60s → 关闭（规避风控）

采集与解析分离：adapter 只负责拿 HTML/元素，交给 parsers 解析。
"""
import logging
import time
import urllib.parse
from typing import Optional

from playwright.sync_api import Page, BrowserContext, TimeoutError as PWTimeout

import config
from app import parsers
from app.models import PlatformResult, Listing, DealRecord

log = logging.getLogger(__name__)

# 贝壳面积档位 → URL 段（实测：a3=70-90㎡）。MVP 用面积档位近似。
# 注：需求要求 ±20%，但页面只支持档位。后端传基准面积，adapter 选最近档位。
AREA_SEGMENTS = [
    (50, "a1"), (70, "a2"), (90, "a3"), (110, "a4"),
    (140, "a5"), (170, "a6"), (9999, "a7"),
]


def _pick_area_segment(area: float) -> str:
    """基准面积 → 最近的贝壳面积档位 URL 段。"""
    for upper, seg in AREA_SEGMENTS:
        if area <= upper:
            return seg
    return "a7"


def collect(page: Page, community_name: str, area: float,
            xiaoqu_id: Optional[str] = None) -> PlatformResult:
    """在已登录的页面上执行贝壳采集。返回单平台结果。

    page: 已登录、停在贝壳某页的主会话页
    xiaoqu_id: 可选，已知小区ID时直接用，否则从搜索结果找
    """
    start = time.time()
    try:
        return _do_collect(page, community_name, area, xiaoqu_id)
    except PWTimeout:
        return PlatformResult(name="贝壳", status="TIMEOUT",
                              reason="采集超时")
    except Exception as e:
        log.exception("贝壳采集异常")
        return PlatformResult(name="贝壳", status="ERROR", reason=str(e))
    finally:
        log.info("贝壳采集耗时 %.1fs", time.time() - start)


def _check_login(page: Page) -> bool:
    """检测是否仍登录（未跳登录页/验证码）。"""
    url = page.url
    if "captcha" in url or "clogin" in url or "login" in url:
        return False
    return True


def _do_collect(page: Page, community_name: str, area: float,
                xiaoqu_id: Optional[str]) -> PlatformResult:
    # --- Step 1: 搜索页（当前 tab）---
    seg = _pick_area_segment(area)
    if xiaoqu_id:
        search_url = f"https://sz.ke.com/ershoufang/{seg}c{xiaoqu_id}/"
    else:
        enc = urllib.parse.quote(community_name)
        search_url = f"https://sz.ke.com/ershoufang/{seg}rs{enc}/"
    log.info("搜索: %s", search_url)
    page.goto(search_url, wait_until="domcontentloaded",
              timeout=config.REQUEST_TIMEOUT_SECONDS * 1000)
    time.sleep(2)

    if not _check_login(page):
        return PlatformResult(name="贝壳", status="LOGIN_EXPIRED",
                              reason="登录态失效，需人工重新登录")

    if "captcha" in page.url:
        return PlatformResult(name="贝壳", status="BLOCKED",
                              reason="搜索页触发验证码")

    # 解析在售房源
    search_html = page.content()
    raw_listings = parsers.parse_listings(search_html)
    listings = [Listing(**l) for l in raw_listings]

    # 找详情链接
    detail_url = parsers.find_detail_link(search_html)
    if not detail_url and xiaoqu_id:
        detail_url = f"https://sz.ke.com/xiaoqu/{xiaoqu_id}/"

    # --- Step 2: 新标签页打开详情页 ---
    community_avg = None
    deals = []
    if detail_url:
        community_avg, deals = _fetch_detail(page.context, detail_url)

    status = "SUCCESS" if (listings or deals or community_avg) else "NO_DATA"
    return PlatformResult(
        name="贝壳", status=status,
        community_avg_price=community_avg,
        listings=listings,
        deals=deals,
    )


def _fetch_detail(context: BrowserContext, detail_url: str):
    """新标签页抓详情页，停留 60s 后关闭。返回 (小区均价, 成交记录列表)。"""
    log.info("打开详情页(新tab): %s", detail_url)
    detail_page = context.new_page()
    try:
        detail_page.goto(detail_url, wait_until="domcontentloaded",
                         timeout=config.REQUEST_TIMEOUT_SECONDS * 1000)
        time.sleep(2)
        if "captcha" in detail_page.url:
            log.warning("详情页被风控")
            return None, []
        html = detail_page.content()
        avg = parsers.parse_community_avg_price(html)
        raw_deals = parsers.parse_deals(html)
        deals = [DealRecord(**d) for d in raw_deals]
        # 停留模拟真人浏览（规避风控）
        log.info("详情页停留 %ds 模拟浏览", config.DETAIL_TAB_LINGER_SECONDS)
        time.sleep(config.DETAIL_TAB_LINGER_SECONDS)
        return avg, deals
    finally:
        detail_page.close()
```

- [ ] **Step 2: 验证可导入**

Run: `cd jeethink-rpa && python -c "from app.ke_adapter import collect; print('OK')"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add app/ke_adapter.py
git commit -m "feat(rpa): 贝壳adapter-搜索/详情采集+风控规避停留"
```

---

## Task 7: 浏览器生命周期管理（常驻+登录+保活）

**Files:**
- Create: `jeethink-rpa/app/browser.py`

- [ ] **Step 1: 实现浏览器管理器**

`app/browser.py`：
```python
# -*- coding: utf-8 -*-
"""浏览器生命周期：启动→人工登录→常驻→保活→健康检查。

设计（用户方案）：成本最低——人工登录一次，浏览器常驻，不维护 cookie 池。
"""
import logging
import threading
import time

from playwright.sync_api import sync_playwright, Browser, BrowserContext, Page

import config

log = logging.getLogger(__name__)


class BrowserSession:
    """常驻浏览器会话。单例，整个服务生命周期复用。"""

    def __init__(self):
        self._pw = None
        self._browser: Browser = None
        self._context: BrowserContext = None
        self._page: Page = None
        self._keepalive_thread = None
        self._keepalive_stop = threading.Event()
        self._logged_in = False

    def start(self, headless: bool = False):
        """启动浏览器并等待人工登录。阻塞直到登录完成。"""
        self._pw = sync_playwright().start()
        self._browser = self._pw.chromium.launch(headless=headless)
        self._context = self._browser.new_context(
            user_agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
                       "AppleWebKit/537.36 (KHTML, like Gecko) "
                       "Chrome/120.0.0.0 Safari/537.36",
            viewport={"width": 1440, "height": 900}, locale="zh-CN")
        self._page = self._context.new_page()
        self._page.goto(config.KE_HOME, wait_until="domcontentloaded")
        log.warning("="*50)
        log.warning("浏览器已打开贝壳。请在浏览器中完成登录（手机验证码）。")
        log.warning("登录完成后，在此输入 yes 确认：")
        log.warning("="*50)
        # MVP：阻塞等待人工确认登录
        while True:
            ans = input("已登录？(yes): ").strip().lower()
            if ans == "yes":
                break
        self._logged_in = True
        self._start_keepalive()
        log.info("登录确认，服务就绪")

    @property
    def page(self) -> Page:
        return self._page

    def is_alive(self) -> bool:
        """会话是否有效（浏览器在 + 未掉登录）。"""
        if not self._browser or not self._page:
            return False
        url = self._page.url or ""
        if "captcha" in url or "clogin" in url:
            return False
        return True

    def _start_keepalive(self):
        """启动后台保活线程：定时做微操作维持会话。"""
        def _loop():
            while not self._keepalive_stop.wait(config.KEEPALIVE_INTERVAL_SECONDS):
                try:
                    # 保活：访问首页或刷新，维持风控信任
                    self._page.goto(config.KE_HOME, wait_until="domcontentloaded")
                    log.info("保活: 刷新首页 (%s)", time.strftime("%H:%M:%S"))
                except Exception as e:
                    log.error("保活失败，可能掉登录: %s", e)
                    self._logged_in = False
        self._keepalive_thread = threading.Thread(target=_loop, daemon=True)
        self._keepalive_thread.start()

    def stop(self):
        self._keepalive_stop.set()
        if self._context: self._context.close()
        if self._browser: self._browser.close()
        if self._pw: self._pw.stop()
        log.info("浏览器会话已关闭")


# 全局单例
session: BrowserSession = BrowserSession()
```

- [ ] **Step 2: 验证可导入**

Run: `cd jeethink-rpa && python -c "from app.browser import session; print('OK')"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add app/browser.py
git commit -m "feat(rpa): 浏览器常驻会话+人工登录+后台保活"
```

---

## Task 8: FastAPI 服务（编排）

**Files:**
- Create: `jeethink-rpa/app/server.py`
- Create: `jeethink-rpa/main.py`

- [ ] **Step 1: 实现服务编排**

`app/server.py`：
```python
# -*- coding: utf-8 -*-
"""FastAPI 服务：编排 adapter + algorithm。

接口：
  GET  /health    健康检查（含登录态）
  POST /inquiry   询价
"""
import logging
import time

from fastapi import FastAPI
from fastapi.responses import JSONResponse

import config
from app.algorithm import filter_by_area, mean_price, decide_final_price
from app.browser import session
from app.ke_adapter import collect
from app.models import InquiryRequest, InquiryResponse, PlatformResult

log = logging.getLogger(__name__)
app = FastAPI(title="融泽源 RPA 询价服务")


@app.get("/health")
def health():
    return {"status": "ok", "logged_in": session.is_alive(),
            "browser_alive": session._browser is not None}


@app.post("/inquiry", response_model=InquiryResponse)
def inquiry(req: InquiryRequest):
    start = time.time()

    # 前置：检查登录态
    if not session.is_alive():
        return InquiryResponse(
            success=False, algorithm_branch="FAILED",
            platforms=[PlatformResult(name="贝壳", status="LOGIN_EXPIRED",
                                       reason="登录态失效，需人工重新登录")],
            elapsed_ms=int((time.time() - start) * 1000))

    # Step 1: 贝壳采集
    pr = collect(session.page, req.community_name, req.area)
    platforms = [pr]

    # 任一平台掉登录 → 直接返回提醒（人工兜底）
    if pr.status == "LOGIN_EXPIRED":
        return InquiryResponse(success=False, algorithm_branch="FAILED",
                               platforms=platforms,
                               elapsed_ms=int((time.time() - start) * 1000))

    # Step 2: 算法计算
    # P_quote：用详情页小区均价（用户拍板）
    quote_avg = pr.community_avg_price
    # P_deal：成交记录按面积±20%筛选后求均值
    deal_items = [{"unit_price": d.unit_price, "area": d.area} for d in pr.deals]
    filtered_deals = filter_by_area(deal_items, req.area, config.AREA_TOLERANCE)
    deal_avg = mean_price(filtered_deals)

    # Step 3: §3.6 取舍
    decision = decide_final_price(quote_avg, deal_avg,
                                  config.DEAL_DIFF_THRESHOLD,
                                  config.DISCOUNT_WHEN_NO_DEAL)

    return InquiryResponse(
        success=decision.branch != "FAILED",
        final_price=decision.final_price,
        algorithm_branch=decision.branch,
        quote_avg=quote_avg,
        deal_avg=deal_avg,
        platforms=platforms,
        elapsed_ms=int((time.time() - start) * 1000))
```

- [ ] **Step 2: 实现入口**

`main.py`：
```python
# -*- coding: utf-8 -*-
"""RPA 服务入口：启动浏览器(人工登录) → 启动 FastAPI。"""
import logging
import sys

import uvicorn

from app.browser import session

logging.basicConfig(level=logging.INFO,
                    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s")


def main():
    # 1. 启动浏览器，阻塞等待人工登录
    headless = "--headless" in sys.argv
    session.start(headless=headless)

    # 2. 启动 FastAPI（登录后才对外服务）
    uvicorn.run("app.server:app", host="0.0.0.0", port=8080, reload=False)


if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        session.stop()
```

- [ ] **Step 3: 验证服务可启动（不连浏览器，仅查 import）**

Run: `cd jeethink-rpa && python -c "from app.server import app; print('routes:', [r.path for r in app.routes])"`
Expected: 输出含 `/health` 和 `/inquiry`

- [ ] **Step 4: Commit**

```bash
git add app/server.py main.py
git commit -m "feat(rpa): FastAPI服务-询价编排+登录态检查兜底"
```

---

## Task 9: 端到端验证（手动，需人工登录）

**Files:** 无（验证现有代码）

- [ ] **Step 1: 启动服务（有头浏览器，人工登录）**

Run: `cd jeethink-rpa && python main.py`
操作：浏览器打开后，手动登录贝壳（手机验证码），在终端输入 `yes` 确认。服务进入就绪态。

- [ ] **Step 2: 健康检查**

Run（新终端）: `curl http://localhost:8080/health`
Expected: `{"status":"ok","logged_in":true,"browser_alive":true}`

- [ ] **Step 3: 发起询价（绿景虹湾，基准面积85）**

Run:
```bash
curl -X POST http://localhost:8080/inquiry \
  -H "Content-Type: application/json" \
  -d '{"community_name":"绿景虹湾","area":85}'
```
Expected（约 70s 后，含详情页停留 60s）:
```json
{
  "success": true,
  "final_price": <数值>,
  "algorithm_branch": "TAKE_LOWER|DEAL_ONLY|QUOTE_DISCOUNT",
  "quote_avg": 85635.0,
  "deal_avg": <数值或null>,
  "platforms": [{"name":"贝壳","status":"SUCCESS",...}],
  "elapsed_ms": <约70000>
}
```

- [ ] **Step 4: 验证算法分支正确性**

根据返回的 `deal_avg` 手工核算：
- 有 `deal_avg`：算 `|quote_avg - deal_avg|/deal_avg`，≤10% 应 `TAKE_LOWER` 且 `final_price=min`，>10% 应 `DEAL_ONLY`
- 无 `deal_avg`：应 `QUOTE_DISCOUNT`，`final_price = quote_avg * 0.8`

- [ ] **Step 5: 验证登录失效兜底**

手动在浏览器登出贝壳，再发一次 `/inquiry`：
Expected: `success=false`，`platforms[0].status="LOGIN_EXPIRED"`，`reason` 含"需人工重新登录"

- [ ] **Step 6: 记录验证结果**

在 `docs/2026-07-09-ke-mvp.md` 末尾追加「验证记录」小节，记下：实际 final_price、算法分支、成交记录条数、是否有风控拦截、详情页成交 DOM 实际 class（用于后续 parser 精调）。

---

## Task 10: 后续 TODO（不在本次 MVP 实现，记录待办）

- [ ] **多标签页并行测试**：验证同一会话开多个搜索 tab 并发处理多请求的风控容忍度（你点3的构想）。需单独测试脚本。
- [ ] **成交记录 DOM 精调**：常驻会话能稳定访问详情页后，根据实际 class 固化 `parse_deals` 的选择器。
- [ ] **其他平台 adapter**：链家/安居客/房天下/乐有家，复用 algorithm + parsers 接口，各写一个 `xx_adapter.py`。
- [ ] **登录态监控告警**：保活失败时，除返回 LOGIN_EXPIRED，可加 webhook/日志告警通知运维。

---

## Self-Review 自检

**1. Spec 覆盖**（对照需求文档）：
- ✅ 搜小区+筛面积 → Task 6 `_do_collect`
- ✅ 报价均值 P_quote（详情页小区均价）→ Task 8 `quote_avg = pr.community_avg_price`
- ✅ 成交记录抓取 → Task 6 `_fetch_detail` + Task 5 `parse_deals`
- ✅ 面积±20%筛选 → Task 3 `filter_by_area` + Task 8 应用到 deals
- ✅ §3.6 取舍算法 → Task 3 `decide_final_price` + Task 8 编排
- ✅ 抓到几个算几个（平台容错）→ Task 6 异常捕获返回状态
- ✅ 常驻+人工登录+保活 → Task 7
- ✅ 过期提醒调用方 → Task 8 LOGIN_EXPIRED 返回
- ✅ 详情页停留60s → Task 6 `_fetch_detail`
- ⏸ 多标签页并行 → Task 10 TODO（用户明确说先做测试项）

**2. 占位符扫描**：无 TBD/TODO（Task 10 是明确的后续待办，非占位）。

**3. 类型一致性**：
- `filter_by_area`/`mean_price`/`decide_final_price` 在 Task 3 定义，Task 8 调用，签名一致 ✅
- `PlatformResult`/`Listing`/`DealRecord` 在 Task 2 定义，Task 6/8 使用一致 ✅
- `collect()` 返回 `PlatformResult`，Task 8 接收一致 ✅
